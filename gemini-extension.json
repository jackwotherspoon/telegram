{
  "name": "telegram",
  "version": "1.0.0",
  "description": "Telegram channel for Gemini CLI — messaging bridge with access control",
  "mcpServers": {
    "telegram": {
      "command": "npx",
      "args": ["tsx", "${extensionPath}${/}server.ts"],
      "cwd": "${extensionPath}",
      "env": {
        "TELEGRAM_BOT_TOKEN": "${TELEGRAM_BOT_TOKEN}"
      }
    }
  },
  "contextFileName": "GEMINI.md",
  "settings": [
    {
      "name": "Telegram Bot Token",
      "description": "Bot token from @BotFather on Telegram",
      "envVar": "TELEGRAM_BOT_TOKEN",
      "sensitive": true
    }
  ]
}
